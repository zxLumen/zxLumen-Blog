# 复制为 .env 并按需修改(生产环境务必改掉这两个密钥)

# SQLite 数据库路径(默认 ./data/zx.db)
DB_PATH=./data/zx.db

# admin 私密留言页密码
ADMIN_PASSWORD=change-me-admin

# DeepSeek 用量上报接口的共享密钥(请求头 X-Report-Token)
REPORT_TOKEN=change-me-report
